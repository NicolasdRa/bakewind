# Database URL (SQLite for development)
DB_URL=sqlite:./src/lib/drizzle/db.sqlite

# Site Configuration
SITE_NAME=BakeWind

# Session Secret (MUST be at least 32 characters)
# Generate with: openssl rand -base64 32
SESSION_SECRET=REPLACE_WITH_SECURE_32_CHAR_SECRET_GENERATED_BY_OPENSSL

# Backend API URL
VITE_API_URL=http://localhost:5000/api/v1

# Admin Dashboard App URL (for post-login redirects)
VITE_ADMIN_APP_URL=http://localhost:3001