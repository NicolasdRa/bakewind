# Website - Proxy Configuration
# Use this when running behind Caddy reverse proxy on localhost:8080

# API URL (accessed through proxy)
VITE_API_URL=http://localhost:8080/api/v1

# Admin app URL (accessed through proxy)
VITE_ADMIN_APP_URL=http://localhost:8080/admin
